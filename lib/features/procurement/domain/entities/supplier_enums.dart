/// Enum representing different types of suppliers
enum SupplierType {
  manufacturer,
  distributor,
  retailer,
  wholesaler,
  service,
  other,
}

/// Enum representing different statuses of suppliers
enum SupplierStatus {
  active,
  inactive,
  pending,
  blacklisted,
}
